version: "3.8"
services:
 db:
  image: mysql:8.0
  environment:
   MYSQL_ROOT_PASSWORD: root
   MYSQL_DATABASE: movies_db
   MYSQL_USER: user
   MYSQL_PASSWORD: secret
  ports:
   - "3307:3306"
  volumes:
   - mysql_data:/var/lib/mysql
volumes:
 mysql_data:
